 private readonly ICurrentRepository _currentRepository;

 public CurrentServices(ICurrentRepository currentRepository, IConfiguration configuration)
 {
     _currentRepository = currentRepository;

 }
